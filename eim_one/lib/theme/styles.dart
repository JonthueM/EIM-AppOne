import 'package:flutter/material.dart';

class globalShadow {
  static const shadowOffSetX = 0.0;
  static const shadowOffsetY = 4.0;
  static const blurRadius = 4.0;
  static const colorShadow = Color.fromRGBO(0, 0, 0, 25);
}