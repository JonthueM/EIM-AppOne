import 'package:flutter/material.dart';

class eimCard extends StatelessWidget {
  const eimCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return GestureDector(
        onTap: () =>....,
        child: Card
    );
  }
}