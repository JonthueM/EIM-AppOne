import 'package:flutter/material.dart';

class quiz_card_container extends StatelessWidget {
  const quiz_card_container({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        
    );
  }
}