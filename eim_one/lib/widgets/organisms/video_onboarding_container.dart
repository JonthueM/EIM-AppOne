import 'package:flutter/material.dart';

class video_onboarding_container extends StatelessWidget {
  const video_onboarding_container({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}