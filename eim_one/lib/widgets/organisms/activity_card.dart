import 'package:flutter/material.dart';

class activity_container extends StatelessWidget {
  const activity_container({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}