import 'package:flutter/material.dart';

class category_section_container extends StatelessWidget {
  const category_section_container({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}