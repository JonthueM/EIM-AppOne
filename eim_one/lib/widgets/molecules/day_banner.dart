import 'package:flutter/material.dart';

class day_banner extends StatelessWidget {
  const day_banner({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}